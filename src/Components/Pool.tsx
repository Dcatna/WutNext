import React from 'react'
import Navbar from '../Pages/Navbar/Navbar'

type Props = {}

const Pool = (props: Props) => {
  return (
    <div>
      <Navbar></Navbar>
      pool
    </div>
  )
}

export default Pool